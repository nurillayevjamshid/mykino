{
  "name": "ntv-telegram-mini-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "redis": "^4.7.0"
  },
  "scripts": {
    "check": "node scripts/check-syntax.js",
    "test": "node scripts/run-tests.js --reporter=spec",
    "test:ci": "node scripts/run-tests.js --reporter=tap",
    "faststart": "node scripts/faststart-drive.js",
    "faststart:apply": "node scripts/faststart-drive.js --apply"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1047.0",
    "@aws-sdk/lib-storage": "^3.1047.0"
  }
}
